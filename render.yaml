services:
  - type: web
    name: devlens-ai-backend
    env: node
    plan: starter
    buildCommand: npm install && npm run build -w @devlens/shared && npm run build -w @devlens/backend
    startCommand: npm run start -w @devlens/backend
    autoDeploy: true
